{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Provedown","text":"<p>Markdown-native verifiable literate documents. Write reports for people, attach small executable evidence to the claims that matter, and let a non-LLM verifier check the numbers.</p> <p>Start the tutorial Read the markup contract</p>"},{"location":"#what-it-does","title":"What It Does","text":"<p>Provedown keeps the human-readable document as the source of truth. Values in prose are authored claims, not generated render output, and verifier plugins can recompute those claims from embedded evidence.</p> <p>Reader view</p> <pre><code>\norders = [\n    {\"status\": \"paid\", \"amount\": 120},\n    {\"status\": \"refunded\", \"amount\": 45},\n    {\"status\": \"paid\", \"amount\": 75},\n]\npaid = [order for order in orders if order[\"status\"] == \"paid\"]\ntotal = sum(order[\"amount\"] for order in paid)\n</code></pre> <p>The report includes 2 paid orders totaling $195.</p> <p>Provedown source</p> <pre><code>&lt;pre&gt;&lt;code&gt;\norders = [\n    {\"status\": \"paid\", \"amount\": 120},\n    {\"status\": \"refunded\", \"amount\": 45},\n    {\"status\": \"paid\", \"amount\": 75},\n]\npaid = [order for order in orders if order[\"status\"] == \"paid\"]\ntotal = sum(order[\"amount\"] for order in paid)\n&lt;/code&gt;&lt;/pre&gt;\n\nThe report includes &lt;span class=\"result\" data-code=\"len(paid)\"&gt;2&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; paid orders totaling &lt;span class=\"result\" data-code=\"f'${total}'\"&gt;$195&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>If the data or filtering logic changes, <code>provedown verify</code> catches stale prose before it ships.</p>"},{"location":"#find-your-path","title":"Find Your Path","text":"<ul> <li> <p>Learn the loop</p> <p>Create a first report, run the verifier, and see how mismatches are reported.</p> <p>First verified report</p> </li> <li> <p>Use real inputs</p> <p>Import standard-library modules, read local CSV files, and verify prose claims against computed summaries.</p> <p>Python modules and data files</p> </li> <li> <p>Inspect and lint</p> <p>Understand which code supports which claims, then catch fragile document structure before verification.</p> <p>Inspect dependencies</p> </li> <li> <p>Check the contract</p> <p>Review the recognized HTML elements, attributes, comparison policies, and CLI behavior.</p> <p>Markup contract</p> </li> <li> <p>Understand hallucination checks</p> <p>See how Provedown turns important prose claims into values that deterministic tools can challenge.</p> <p>Hallucination detection</p> </li> </ul>"},{"location":"#documentation-map","title":"Documentation Map","text":"<p>This documentation follows the Diataxis structure:</p> <ul> <li>Tutorials teach new workflows step by step.</li> <li>How-to guides solve focused operational tasks.</li> <li>Reference describes stable APIs, commands, and file formats.</li> <li>Explanations discuss design choices and tradeoffs.</li> </ul> <p>The project is still early, so examples and reference pages describe what exists today while design notes track where the format is heading.</p>"},{"location":"examples/basic-report/","title":"Basic Verified Report","text":"<p>A compact Provedown document with inline Python, scalar result assertions, set comparison, tolerance comparison, and a named expression.</p> <p>The tabs show the same Provedown document in two forms. The rendered view is generated from the raw source with <code>pandoc</code>; the raw view shows the literal Provedown contract.</p> RenderedRaw .md <p> </p> <pre><code># Basic Verified Report\n\nThis Provedown document checks a small in-memory cohort.\n\n&lt;pre&gt;&lt;code&gt;\nsamples = [\"alpha\", \"beta\", \"gamma\"]\npassed = [name for name in samples if \"a\" in name]\nrate = len(passed) / len(samples)\n&lt;/code&gt;&lt;/pre&gt;\n\nThe cohort has &lt;span class=\"result\" data-code=\"len(samples)\"&gt;3&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; samples.\n\nThe passing labels are &lt;span class=\"result\" data-code=\"passed\" data-compare=\"set\"&gt;alpha,beta,gamma&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n\nThe pass rate is &lt;span class=\"result\" data-code=\"rate\" tol=\"1e-6\"&gt;1.0&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n\n&lt;pre&gt;&lt;code name=\"summary\"&gt;f\"{len(passed)}/{len(samples)}\"&lt;/code&gt;&lt;/pre&gt;\n\nThe compact summary is &lt;span class=\"result\" data-code=\"#summary\"&gt;3/3&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>Verify this example with:</p> <pre><code>provedown verify examples/basic-report.md\n</code></pre>"},{"location":"examples/basic-report/#basic-verified-report","title":"Basic Verified Report","text":"<p>This Provedown document checks a small in-memory cohort.</p> <pre><code>\nsamples = [\"alpha\", \"beta\", \"gamma\"]\npassed = [name for name in samples if \"a\" in name]\nrate = len(passed) / len(samples)\n</code></pre> <p>The cohort has 3 samples.</p> <p>The passing labels are alpha,beta,gamma.</p> <p>The pass rate is 1.0.</p> <pre><code>f\"{len(passed)}/{len(samples)}\"</code></pre> <p>The compact summary is 3/3.</p>"},{"location":"examples/data-file-report/","title":"CSV Sales Summary","text":"<p>A more realistic Provedown document that imports Python standard-library modules, reads a local CSV file, and verifies summary claims in prose.</p> <p>The tabs show the same Provedown document in two forms. The rendered view is generated from the raw source with <code>pandoc</code>; the raw view shows the literal Provedown contract.</p> RenderedRaw .md <p>"},{"location":"examples/data-file-report/#csv-sales-summary","title":"CSV Sales Summary","text":"<p>This Provedown document imports Python standard-library modules, reads a data file next to the report, computes summary values, and asserts those values in the prose.</p> <p>The source data is <code>data/orders.csv</code>.</p> <pre><code>\nfrom collections import Counter\nfrom csv import DictReader\nfrom decimal import Decimal\nfrom pathlib import Path\n<p>data_path = Path(\"data/orders.csv\")\nwith data_path.open(encoding=\"utf-8\", newline=\"\") as stream:\n    rows = list(DictReader(stream))</p>\n<p>paid_rows = [row for row in rows if row[\"status\"] == \"paid\"]\npaid_total = sum(Decimal(row[\"amount\"]) for row in paid_rows)\npaid_average = paid_total / len(paid_rows)\norders_by_region = Counter(row[\"region\"] for row in rows)\npaid_regions = sorted({row[\"region\"] for row in paid_rows})\nlargest_order = max(rows, key=lambda row: Decimal(row[\"amount\"]))\n\n<p>The file contains 6 orders.</p>\n<p>Of those, 4 are paid orders.</p>\n<p>Paid orders total 461.00.</p>\n<p>The average paid order is 115.25.</p>\n<p>The North region has 2 orders.</p>\n<p>Paid orders appear in the regions North,South,West.</p>\n<p>The largest order id is A004.</p>\n<pre><code>f\"{len(paid_rows)} paid orders totaling ${paid_total}\"</code></pre>\n<p>The compact topline is 4 paid orders totaling $461.00.</p>\n</p>\n\n\n<pre><code># CSV Sales Summary\n\nThis Provedown document imports Python standard-library modules, reads a data\nfile next to the report, computes summary values, and asserts those values in\nthe prose.\n\nThe source data is [`data/orders.csv`](data/orders.csv).\n\n&lt;pre&gt;&lt;code&gt;\nfrom collections import Counter\nfrom csv import DictReader\nfrom decimal import Decimal\nfrom pathlib import Path\n\ndata_path = Path(\"data/orders.csv\")\nwith data_path.open(encoding=\"utf-8\", newline=\"\") as stream:\n    rows = list(DictReader(stream))\n\npaid_rows = [row for row in rows if row[\"status\"] == \"paid\"]\npaid_total = sum(Decimal(row[\"amount\"]) for row in paid_rows)\npaid_average = paid_total / len(paid_rows)\norders_by_region = Counter(row[\"region\"] for row in rows)\npaid_regions = sorted({row[\"region\"] for row in paid_rows})\nlargest_order = max(rows, key=lambda row: Decimal(row[\"amount\"]))\n&lt;/code&gt;&lt;/pre&gt;\n\nThe file contains &lt;span class=\"result\" data-code=\"len(rows)\"&gt;6&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; orders.\n\nOf those, &lt;span class=\"result\" data-code=\"len(paid_rows)\"&gt;4&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; are paid orders.\n\nPaid orders total &lt;span class=\"result\" data-code=\"paid_total\"&gt;461.00&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n\nThe average paid order is &lt;span class=\"result\" data-code=\"paid_average\"&gt;115.25&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n\nThe North region has &lt;span class=\"result\" data-code=\"orders_by_region['North']\"&gt;2&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; orders.\n\nPaid orders appear in the regions &lt;span class=\"result\" data-code=\"paid_regions\" data-compare=\"set\"&gt;North,South,West&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n\nThe largest order id is &lt;span class=\"result\" data-code=\"largest_order['order_id']\"&gt;A004&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n\n&lt;pre&gt;&lt;code name=\"topline\"&gt;f\"{len(paid_rows)} paid orders totaling ${paid_total}\"&lt;/code&gt;&lt;/pre&gt;\n\nThe compact topline is &lt;span class=\"result\" data-code=\"#topline\"&gt;4 paid orders totaling $461.00&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre>\n\n\n\n<p>Verify this example with:</p>\n<pre><code>provedown verify examples/data-file-report.md\n</code></pre>"},{"location":"examples/html-report/","title":"Pure HTML Report","text":"<p>A Provedown document written as plain HTML, useful when a pipeline already emits HTML or when Markdown is not part of the authoring workflow.</p> <p>The tabs show the same Provedown document in two forms. The rendered view is generated from the raw source with <code>pandoc</code>; the raw view shows the literal Provedown contract.</p> RenderedRaw .html <p>"},{"location":"examples/html-report/#inventory-check","title":"Inventory Check","text":"<p>This report is authored as plain HTML. Provedown reads the same executable code and result assertion contract it reads from Markdown.</p> <pre><code>from statistics import mean\n<p>counts = [12, 18, 15]\nmean_count = mean(counts)\n    \n<p>The average count is 15.0.</p>\n<p>The maximum count is 18.</p>\n</p>\n\n\n<pre><code>&lt;!doctype html&gt;\n&lt;html lang=\"en\"&gt;\n  &lt;head&gt;\n    &lt;meta charset=\"utf-8\"&gt;\n    &lt;title&gt;Inventory Provedown Report&lt;/title&gt;\n  &lt;/head&gt;\n  &lt;body&gt;\n    &lt;h1&gt;Inventory Check&lt;/h1&gt;\n    &lt;p&gt;\n      This report is authored as plain HTML. Provedown reads the same\n      executable code and result assertion contract it reads from Markdown.\n    &lt;/p&gt;\n\n    &lt;pre&gt;&lt;code&gt;\nfrom statistics import mean\n\ncounts = [12, 18, 15]\nmean_count = mean(counts)\n    &lt;/code&gt;&lt;/pre&gt;\n\n    &lt;p&gt;\n      The average count is\n      &lt;span class=\"result\" data-code=\"f'{mean_count:.1f}'\"&gt;15.0&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n    &lt;/p&gt;\n\n    &lt;p&gt;\n      The maximum count is\n      &lt;span class=\"result\" data-code=\"max(counts)\"&gt;18&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n    &lt;/p&gt;\n  &lt;/body&gt;\n&lt;/html&gt;\n</code></pre>\n\n\n\n<p>Verify this example with:</p>\n<pre><code>provedown verify examples/html-report.html\n</code></pre>"},{"location":"examples/sql-sales-summary/","title":"SQL CSV Sales Summary","text":"<p>A Provedown document that uses DuckDB SQL to query a local CSV file and verify summary claims in prose.</p> <p>The tabs show the same Provedown document in two forms. The rendered view is generated from the raw source with <code>pandoc</code>; the raw view shows the literal Provedown contract.</p> RenderedRaw .md <p> </p> <pre><code># SQL CSV Sales Summary\n\nThis Provedown document verifies CSV summary claims with DuckDB SQL.\n\n&lt;pre&gt;&lt;code data-language=\"sql\"&gt;\ncreate view orders as\nselect *\nfrom read_csv_auto('data/orders.csv');\n&lt;/code&gt;&lt;/pre&gt;\n\nThe CSV contains &lt;span class=\"result\" data-language=\"sql\" data-code=\"select count(*) from orders\"&gt;6&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; orders.\n\nThere are &lt;span class=\"result\" data-language=\"sql\" data-code=\"select count(*) from orders where status = 'paid'\"&gt;4&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; paid orders.\n\nPaid orders total &lt;span class=\"result\" data-language=\"sql\" data-code=\"select printf('$%.2f', sum(amount)) from orders where status = 'paid'\"&gt;$461.00&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n\nThe paid regions are &lt;span class=\"result\" data-language=\"sql\" data-compare=\"set\" data-code=\"select distinct region from orders where status = 'paid'\"&gt;North,South,West&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n\n&lt;pre&gt;&lt;code name=\"sql_topline\" data-language=\"sql\"&gt;\nselect count(*)::varchar || ' paid orders totaling ' || printf('$%.2f', sum(amount))\nfrom orders\nwhere status = 'paid'\n&lt;/code&gt;&lt;/pre&gt;\n\nThe SQL topline is &lt;span class=\"result\" data-language=\"sql\" data-code=\"#sql_topline\"&gt;4 paid orders totaling $461.00&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>Verify this example with:</p> <pre><code>provedown verify examples/sql-sales-summary.md\n</code></pre>"},{"location":"examples/sql-sales-summary/#sql-csv-sales-summary","title":"SQL CSV Sales Summary","text":"<p>This Provedown document verifies CSV summary claims with DuckDB SQL.</p> <pre><code>\ncreate view orders as\nselect *\nfrom read_csv_auto('data/orders.csv');\n</code></pre> <p>The CSV contains 6 orders.</p> <p>There are 4 paid orders.</p> <p>Paid orders total $461.00.</p> <p>The paid regions are North,South,West.</p> <pre><code>\nselect count(*)::varchar || ' paid orders totaling ' || printf('$%.2f', sum(amount))\nfrom orders\nwhere status = 'paid'\n</code></pre> <p>The SQL topline is 4 paid orders totaling $461.00.</p>"},{"location":"explanations/hallucination-detection/","title":"Hallucination Detection","text":"<p>Provedown is designed around a narrow but useful question:</p> <p>Does this readable document contain claims that a deterministic checker can falsify?</p> <p>That is different from asking whether an AI system is generally truthful. A verifier cannot prove that a report is complete, well interpreted, or scientifically sound. It can do something more mechanical: recompute or re-fetch specific claims and report when the document disagrees with the evidence.</p>"},{"location":"explanations/hallucination-detection/#the-pattern","title":"The Pattern","text":"<p>The core pattern is:</p> <ol> <li>Write the claim in the document.</li> <li>Attach the evidence needed to check it.</li> <li>Choose a deterministic comparison policy.</li> <li>Run a non-LLM verifier.</li> </ol> <p>For computed values, the evidence is code:</p> <pre><code>&lt;code&gt;\nrows = load_rows(\"data/orders.csv\")\npaid_rows = [row for row in rows if row[\"status\"] == \"paid\"]\n&lt;/code&gt;\n\nThe report includes &lt;span class=\"result\" data-code=\"len(paid_rows)\"&gt;4&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; paid orders.\n</code></pre> <p>The authored value <code>4</code> is the claim. The code is evidence. The verifier executes the code and compares the recomputed value with the authored text.</p> <p>The same pattern applies beyond numbers. For identifiers, the evidence may be an authoritative ontology or registry. For citations, the evidence may be a publication database. For quoted support, the evidence may be the source text itself.</p>"},{"location":"explanations/hallucination-detection/#relation-to-identifier-validation","title":"Relation To Identifier Validation","text":"<p>The AI4Curators guide on making identifiers hallucination-resistant describes a practical rule for AI-assisted curation: require more than a plausible-looking identifier. Require the identifier and a second piece of information that must agree with an authoritative source, such as the canonical label.</p> <p>That is the same design pressure Provedown applies to prose claims. A claim should not stand alone when it can be paired with checkable evidence.</p> <p>Examples:</p> Claim type Authored claim Evidence Check Count <code>412 samples</code> Python over a data file Recompute count Ontology term <code>GO:0005515 protein binding</code> Ontology lookup ID exists and label matches Publication <code>PMID:10802651 Gene Ontology...</code> PubMed metadata PMID exists and title matches Quote Supporting text plus citation Publication text Excerpt appears in source <p>The common idea is consistency checking. A model may produce something that looks valid. It is harder for it to produce multiple independently checked pieces that all agree with the relevant authority.</p>"},{"location":"explanations/hallucination-detection/#detection-not-generation","title":"Detection, Not Generation","text":"<p>Provedown deliberately treats the document value as authored source, not as rendered output.</p> <p>That distinction matters. If a report always renders a value fresh from code, then there is no separate claim for a verifier to disagree with. The rendered number simply becomes whatever the current code produces.</p> <p>Provedown keeps the human-readable value in the document:</p> <pre><code>The cohort has &lt;span class=\"result\" data-code=\"len(cohort)\"&gt;412&lt;/span&gt; samples.\n</code></pre> <p>If <code>len(cohort)</code> later evaluates to <code>410</code>, the verifier can say the document is false or stale. That mismatch is the hallucination signal.</p>"},{"location":"explanations/hallucination-detection/#what-provedown-can-catch","title":"What Provedown Can Catch","text":"<p>The built-in Python verifier can catch:</p> <ul> <li>fabricated or miscopied scalar values;</li> <li>stale prose after data or code changes;</li> <li>result expressions that raise errors;</li> <li>wrong comparison policies;</li> <li>broken named-code references.</li> </ul> <p>The inspection and linting commands add static checks before execution:</p> <ul> <li><code>provedown inspect</code> shows which code supports which claims;</li> <li><code>provedown lint</code> flags unresolved references, unused named code, unjustified   unverified claims, and fragile Python patterns such as randomness or wall-clock   time.</li> </ul> <p>Future verifier plugins can extend the same model to other hallucination-prone claims:</p> <ul> <li>ontology IDs and labels;</li> <li>gene, protein, chemical, and disease identifiers;</li> <li>publication IDs and titles;</li> <li>quoted text and cited sources;</li> <li>schema-backed references in structured data.</li> </ul>"},{"location":"explanations/hallucination-detection/#what-it-cannot-catch","title":"What It Cannot Catch","text":"<p>Verification is not a substitute for review.</p> <p>Provedown cannot catch a bad claim if the attached code faithfully computes the same bad claim. It cannot know that the wrong dataset was loaded, that a statistical method was inappropriate, or that an important caveat was omitted.</p> <p>A verifier also depends on the quality of its authority:</p> <ul> <li>Computed claims depend on the code, data, and environment.</li> <li>Identifier claims depend on the registry or ontology source.</li> <li>Citation claims depend on the publication metadata source.</li> <li>Quote claims depend on access to the relevant source text.</li> </ul> <p>When those inputs are wrong or incomplete, deterministic validation can still pass the wrong thing.</p>"},{"location":"explanations/hallucination-detection/#why-deterministic-checks-matter","title":"Why Deterministic Checks Matter","text":"<p>Hallucination detection should not rely on another language model deciding whether the first language model sounded right.</p> <p>The strongest checks are boring:</p> <ul> <li>exact equality for stable scalar values;</li> <li>numeric tolerance only when declared;</li> <li>ID existence checks against authoritative APIs;</li> <li>canonical label matching;</li> <li>deterministic substring checks for quoted support;</li> <li>explicit skips for values that are intentionally out of scope.</li> </ul> <p>This is why Provedown treats <code>data-compare=\"none\"</code> as visible metadata rather than silently accepting unverified text. An unverified claim may be legitimate, but it should be countable and reviewable.</p>"},{"location":"explanations/hallucination-detection/#design-consequence","title":"Design Consequence","text":"<p>Provedown's job is not to make AI write better prose. Its job is to make the prose expose enough structure that ordinary tools can challenge it.</p> <p>That leads to a simple design rule:</p> <p>Every important claim should either be verifiable, explicitly unverified, or obviously outside the scope of mechanical checking.</p> <p>The current implementation starts with Python-backed scalar claims because they are common and easy to check. The broader architecture is verifier-neutral so identifier, citation, and reference validators can use the same document model.</p>"},{"location":"explanations/hallucination-detection/#sources","title":"Sources","text":"<ul> <li>Make identifiers hallucination-resistant</li> <li>Verifiable Literate Weave design spec</li> </ul>"},{"location":"explanations/why-not-marimo/","title":"Why Not Marimo?","text":"<p>marimo is one of the closest pieces of prior art to Provedown. It is a reactive Python notebook stored as a Python file, with strong answers for many problems that make traditional notebooks difficult to review, rerun, and deploy.</p> <p>Provedown does not reject marimo's premise. It rejects a different default: that the notebook should be the primary artifact.</p>"},{"location":"explanations/why-not-marimo/#the-short-version","title":"The Short Version","text":"<p>Use marimo when the primary thing you want is a reliable computational notebook or an interactive Python app.</p> <p>Use Provedown when the primary thing you want is a readable document whose claims can be independently checked.</p> <p>marimo keeps computation honest. Provedown keeps prose claims honest.</p>"},{"location":"explanations/why-not-marimo/#different-trust-contracts","title":"Different Trust Contracts","text":"<p>marimo's central guarantee is state consistency. It statically analyzes cells, builds a dependency graph, and reruns cells affected by changes. That eliminates a common notebook failure mode: a visible output that no longer matches the code currently on the page.</p> <p>Provedown's central guarantee is claim falsification. The authored value in prose is the thing being checked:</p> <pre><code>The cohort has &lt;span class=\"result\" data-code=\"len(df)\"&gt;412&lt;/span&gt; samples.\n</code></pre> <p>The verifier reruns the supporting code and asks whether the document's authored claim is still true. The value is not merely regenerated at render time. It is a human-readable claim with attached executable evidence.</p> <p>That distinction matters for agent-written reports. If an agent fabricates or miscopies a number, a notebook can still look plausible. Provedown treats the printed number as an assertion and gives a dumb, non-LLM verifier a narrow job: prove that exact assertion wrong or pass it.</p>"},{"location":"explanations/why-not-marimo/#why-markdown-is-the-source","title":"Why Markdown Is The Source","text":"<p>marimo notebooks are Git-friendly because they are stored as Python files. That is a major improvement over JSON notebooks.</p> <p>Provedown goes further in a different direction: the source is normal Markdown with a small HTML contract. The document should remain useful in ordinary Markdown renderers, code review tools, static sites, and plain text editors. The computation is annotation attached to the prose, not the container that owns the prose.</p> <p>That makes the default reader experience different:</p> <ul> <li>In marimo, the reader is still looking at a notebook or an app derived from a   notebook.</li> <li>In Provedown, the reader is looking at a document. The notebook-like behavior   exists only to verify claims.</li> </ul>"},{"location":"explanations/why-not-marimo/#why-not-reactive-execution","title":"Why Not Reactive Execution?","text":"<p>marimo's reactive dependency graph is the right answer for interactive exploration. If a cell changes, dependent cells should update automatically.</p> <p>For Provedown, the default execution model is intentionally simpler:</p> <ol> <li>Code blocks execute in document order.</li> <li>State accumulates linearly.</li> <li>A result span is checked against the live state at its position in the    document.</li> </ol> <p>This is less powerful than a DAG, but it is easier to audit. A reviewer can read the document from top to bottom and understand what state exists at each claim. When prose order must differ from execution order, Provedown should make that escape hatch explicit rather than implicit.</p> <p>Reactive execution also changes the authoring model. It encourages a document to be organized around dependencies. Provedown is organized around the reader's path through the argument.</p>"},{"location":"explanations/why-not-marimo/#why-not-generated-inline-values","title":"Why Not Generated Inline Values?","text":"<p>Tools such as notebooks, Quarto, knitr, and marimo can display values produced by code. That is useful, but it usually makes the displayed value a render product.</p> <p>Provedown makes the displayed value authored source.</p> <p>That inversion is the core anti-hallucination move. A verifier can disagree with the document because the document contains a claim independent of the recomputed value. If the rendered value is always generated fresh, there is no separate authored claim to falsify.</p>"},{"location":"explanations/why-not-marimo/#where-marimo-could-help","title":"Where Marimo Could Help","text":"<p>marimo is still useful prior art for Provedown. Several ideas are worth borrowing:</p> <ul> <li>Inline dependency metadata with   PEP 723 for single-file reproducibility.</li> <li>Sandboxed execution with <code>uv</code> for verifier isolation.</li> <li>Dependency inspection tools that show which code supports which claims.</li> <li>App or WASM export ideas for richer interactive presentations.</li> <li>Strong linting around mutation, hidden state, and nondeterministic execution.</li> </ul> <p>The likely workflow is not \"Provedown instead of marimo\" in every context. A good workflow may be:</p> <ol> <li>Explore interactively in marimo.</li> <li>Extract the final argument into Provedown.</li> <li>Commit the Markdown report and verify it in CI.</li> </ol>"},{"location":"explanations/why-not-marimo/#decision","title":"Decision","text":"<p>Provedown should not become a marimo wrapper, and it should not adopt marimo's reactive notebook model as its default execution semantics.</p> <p>The project should stay markdown-native, document-first, and verifier-neutral. marimo remains an important comparison point and a source of implementation ideas, but Provedown's differentiator is the falsifiable prose claim.</p>"},{"location":"explanations/why-not-marimo/#sources","title":"Sources","text":"<ul> <li>marimo documentation</li> <li>marimo reactivity guide</li> <li>marimo app guide</li> <li>marimo package reproducibility guide</li> <li>Provedown Verifiable Literate Weave design spec</li> </ul>"},{"location":"how-to-guides/inspect-claim-dependencies/","title":"Inspect Claim Dependencies","text":"<p>Use <code>provedown inspect</code> when you want to understand the static relationships between code blocks, code uses, and result assertions without executing the document.</p> <p>Inspection is useful before verification when you want to answer questions such as:</p> <ul> <li>Which named code blocks are referenced by prose claims?</li> <li>Which code blocks execute inline, and which are deferred to a use site or   result assertion?</li> <li>Does a result point at a missing named expression?</li> <li>Are any named blocks never referenced?</li> </ul>"},{"location":"how-to-guides/inspect-claim-dependencies/#inspect-a-document","title":"Inspect A Document","text":"<p>Run:</p> <pre><code>provedown inspect examples/data-file-report.md\n</code></pre> <p>Text output starts with a summary and then lists document events in document order:</p> <pre><code>examples/data-file-report.md: ok\n  events=10, errors=0, warnings=0\n  [1] code examples/data-file-report.md:9:6 execution='inline'\n  [2] result examples/data-file-report.md:28:19 compare='exact' authored='6' execution='eval'\n</code></pre> <p>The exact line and column numbers may change as the document changes.</p>"},{"location":"how-to-guides/inspect-claim-dependencies/#use-json-output","title":"Use JSON Output","text":"<p>Use JSON when an editor, CI job, or script needs structured inspection data:</p> <pre><code>provedown inspect --format json examples/data-file-report.md\n</code></pre> <p>Each event includes its kind, source location, language, and any relevant reference information. Result assertions include the authored value and comparison policy.</p>"},{"location":"how-to-guides/inspect-claim-dependencies/#interpret-issues","title":"Interpret Issues","text":"<p>Inspection can report errors and warnings.</p> <p>Errors mean the document has a broken static relationship, such as a code use or result assertion pointing at a missing named block. The command exits with status <code>1</code> when any inspection error is present.</p> <p>Warnings identify suspicious but technically executable structure. For example, a named code block that is never referenced is reported as <code>unused-named-code</code>.</p> <p>Inspection does not execute Python, read data files, or check authored values. Use <code>provedown verify</code> for that.</p>"},{"location":"how-to-guides/lint-a-document/","title":"Lint A Document","text":"<p>Use <code>provedown lint</code> to catch fragile document structure without executing code. Linting is static: it parses the document, inspects code/result relationships, and looks for patterns that may make verification misleading or hard to audit.</p>"},{"location":"how-to-guides/lint-a-document/#run-lints","title":"Run Lints","text":"<pre><code>provedown lint report.md\n</code></pre> <p>The command prints one summary per file:</p> <pre><code>report.md: ok\n  errors=0, warnings=1\n  [warning] unused-named-code report.md:3:6: named code block is never referenced by a code use or result assertion target='load'\n</code></pre> <p>Warnings do not make the command fail. Errors do.</p>"},{"location":"how-to-guides/lint-a-document/#use-json-output","title":"Use JSON Output","text":"<pre><code>provedown lint --format json report.md\n</code></pre> <p>JSON output is intended for CI, editor integrations, and scripts.</p>"},{"location":"how-to-guides/lint-a-document/#current-lints","title":"Current Lints","text":"<code>unresolved-code-use</code> A <code>&lt;code use=\"...\"/&gt;</code> references a missing named code block. This is an   error. <code>unresolved-result-reference</code> A result assertion such as <code>data-code=\"#name\"</code> references a missing named code   block. This is an error. <code>unused-named-code</code> A named code block is never referenced by a use site or result assertion. This   is a warning. <code>unjustified-none-compare</code> A result uses <code>data-compare=\"none\"</code> without <code>data-reason</code> or   <code>data-justification</code>. This is a warning. <code>excessive-none-compare</code> A document has more than three unverified result assertions. This is a   warning. <code>fragile-python</code> Python code appears to use randomness, wall-clock time, environment variables,   or network calls during verification. This is a warning. <code>python-syntax-error</code> A Python code block or inline expression cannot be parsed. This is an error."},{"location":"how-to-guides/lint-a-document/#justify-explicitly-unverified-claims","title":"Justify Explicitly Unverified Claims","text":"<p>When a claim is intentionally outside the verifier's scope, mark it with <code>data-compare=\"none\"</code> and explain why:</p> <pre><code>The service status is &lt;span class=\"result\" data-code=\"status()\" data-compare=\"none\" data-reason=\"external service changes between runs\"&gt;available&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>The linter does not judge whether the reason is good. It makes the escape hatch visible and reviewable.</p>"},{"location":"how-to-guides/lint-a-document/#lint-before-verify","title":"Lint Before Verify","text":"<p>Linting does not replace verification:</p> <pre><code>provedown lint report.md\nprovedown verify report.md\n</code></pre> <p>Use linting to catch fragile structure. Use verification to recompute authored claims and compare the results.</p>"},{"location":"how-to-guides/reuse-named-code/","title":"Reuse Named Code","text":"<p>Use named code when an expression is too long for a <code>data-code</code> attribute or when prose order should differ from execution order.</p>"},{"location":"how-to-guides/reuse-named-code/#name-a-result-expression","title":"Name A Result Expression","text":"<p>For longer expressions, put the expression in a named <code>&lt;code&gt;</code> block and point a result span at it with <code>data-code=\"#name\"</code>:</p> <pre><code>&lt;code&gt;\nsamples = [\"alpha\", \"beta\", \"gamma\"]\n&lt;/code&gt;\n\nThe summary is &lt;span class=\"result\" data-code=\"#summary\"&gt;3 samples&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n\n&lt;code name=\"summary\"&gt;f\"{len(samples)} samples\"&lt;/code&gt;\n</code></pre> <p>Named code used by a result assertion must be a Python expression, because the Python verifier evaluates it with <code>eval()</code>.</p> <p>The named block is evaluated at the result site. Its position in the document is where the expression is defined, not where it executes.</p>"},{"location":"how-to-guides/reuse-named-code/#execute-a-named-cell-at-a-use-site","title":"Execute A Named Cell At A Use Site","text":"<p>Use <code>&lt;code use=\"name\"/&gt;</code> when the named block contains statements that should execute somewhere else in the document:</p> <pre><code>&lt;code name=\"load-data\"&gt;\nsamples = [\"alpha\", \"beta\", \"gamma\"]\nsample_count = len(samples)\n&lt;/code&gt;\n\n&lt;code use=\"load-data\"/&gt;\n\nThe cohort has &lt;span class=\"result\" data-code=\"sample_count\"&gt;3&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; samples.\n</code></pre> <p>The use site is the execution site. This keeps execution linear while allowing definitions to be parked away from the prose that needs them.</p>"},{"location":"how-to-guides/reuse-named-code/#avoid-ambiguous-reuse","title":"Avoid Ambiguous Reuse","text":"<p>Prefer one named block per concept. A duplicate <code>name</code> is accepted, but the later definition wins and the parser emits a diagnostic. In practice, duplicate names should be treated as authoring mistakes.</p>"},{"location":"how-to-guides/reuse-named-code/#choose-inline-or-named-code","title":"Choose Inline Or Named Code","text":"<p>Use inline <code>data-code</code> when the expression is short and readable:</p> <pre><code>&lt;span class=\"result\" data-code=\"len(samples)\"&gt;3&lt;/span&gt;\n</code></pre> <p>Use named result expressions when the expression is long but side-effect-free.</p> <p>Use named cells with <code>&lt;code use=\"...\"/&gt;</code> when the code is a setup step or has statements that need to run before later claims.</p>"},{"location":"how-to-guides/use-python-modules-and-data-files/","title":"Use Python Modules And Data Files","text":"<p>Use this pattern when a report needs ordinary Python imports, local input files, and several prose claims derived from the same setup code.</p>"},{"location":"how-to-guides/use-python-modules-and-data-files/#put-data-next-to-the-document","title":"Put Data Next To The Document","text":"<p>For a document at <code>reports/sales.md</code>, keep small example data nearby:</p> <pre><code>reports/\n  sales.md\n  data/\n    orders.csv\n</code></pre> <p>When you run <code>provedown verify reports/sales.md</code>, the Python verifier executes with <code>reports/</code> as the working directory. Relative paths in code blocks are therefore resolved relative to the document, not relative to the shell command's current directory.</p>"},{"location":"how-to-guides/use-python-modules-and-data-files/#import-standard-library-modules-normally","title":"Import Standard-Library Modules Normally","text":"<p>Write ordinary Python in a <code>&lt;code&gt;</code> block:</p> <pre><code>&lt;pre&gt;&lt;code&gt;\nfrom collections import Counter\nfrom csv import DictReader\nfrom decimal import Decimal\nfrom pathlib import Path\n\ndata_path = Path(\"data/orders.csv\")\nwith data_path.open(encoding=\"utf-8\", newline=\"\") as stream:\n    rows = list(DictReader(stream))\n\npaid_rows = [row for row in rows if row[\"status\"] == \"paid\"]\npaid_total = sum(Decimal(row[\"amount\"]) for row in paid_rows)\norders_by_region = Counter(row[\"region\"] for row in rows)\n&lt;/code&gt;&lt;/pre&gt;\n</code></pre> <p>The <code>&lt;pre&gt;&lt;code&gt;</code> wrapper renders nicely in MkDocs and still gives Provedown a <code>&lt;code&gt;</code> element to execute.</p>"},{"location":"how-to-guides/use-python-modules-and-data-files/#assert-the-values-in-prose","title":"Assert The Values In Prose","text":"<p>Attach short expressions directly to result spans:</p> <pre><code>The file contains &lt;span class=\"result\" data-code=\"len(rows)\"&gt;6&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; orders.\n\nPaid orders total &lt;span class=\"result\" data-code=\"paid_total\"&gt;461.00&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n\nThe North region has &lt;span class=\"result\" data-code=\"orders_by_region['North']\"&gt;2&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; orders.\n</code></pre> <p>The authored values, such as <code>6</code>, <code>461.00</code>, and <code>2</code>, are the claims being verified. The code is the evidence used to falsify or pass those claims.</p>"},{"location":"how-to-guides/use-python-modules-and-data-files/#use-named-expressions-for-longer-claims","title":"Use Named Expressions For Longer Claims","text":"<p>When the expression is too long for an attribute, put it in a named code block and reference it with <code>data-code=\"#name\"</code>:</p> <pre><code>&lt;pre&gt;&lt;code name=\"topline\"&gt;f\"{len(paid_rows)} paid orders totaling ${paid_total}\"&lt;/code&gt;&lt;/pre&gt;\n\nThe compact topline is &lt;span class=\"result\" data-code=\"#topline\"&gt;4 paid orders totaling $461.00&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>Named code referenced by a result assertion must be an expression because the Python verifier evaluates it with <code>eval()</code>.</p>"},{"location":"how-to-guides/use-python-modules-and-data-files/#compare-unordered-values-as-sets","title":"Compare Unordered Values As Sets","text":"<p>Use <code>data-compare=\"set\"</code> when order should not matter:</p> <pre><code>Paid orders appear in the regions &lt;span class=\"result\" data-code=\"paid_regions\" data-compare=\"set\"&gt;North,South,West&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>The authored value can be comma-separated text. The computed value can be a Python set, list, tuple, dict, or string representation of a collection.</p>"},{"location":"how-to-guides/use-python-modules-and-data-files/#verify-the-example","title":"Verify The Example","text":"<p>The docs include a complete version of this pattern:</p> <pre><code>provedown verify examples/data-file-report.md\n</code></pre> <p>That example uses only Python's standard library and the checked-in CSV file at <code>examples/data/orders.csv</code>.</p>"},{"location":"how-to-guides/use-sql-and-csv-files/","title":"Use SQL And CSV Files","text":"<p>Use <code>data-language=\"sql\"</code> to verify prose claims with DuckDB SQL. DuckDB can query nearby CSV files directly with <code>read_csv_auto()</code>.</p>"},{"location":"how-to-guides/use-sql-and-csv-files/#create-a-sql-setup-cell","title":"Create A SQL Setup Cell","text":"<p>Create a view over a CSV file:</p> <pre><code>&lt;pre&gt;&lt;code data-language=\"sql\"&gt;\ncreate view orders as\nselect *\nfrom read_csv_auto('data/orders.csv');\n&lt;/code&gt;&lt;/pre&gt;\n</code></pre> <p>When the document is verified from disk, relative paths resolve from the document's directory.</p>"},{"location":"how-to-guides/use-sql-and-csv-files/#write-sql-result-assertions","title":"Write SQL Result Assertions","text":"<p>Put a SQL query in <code>data-code</code>:</p> <pre><code>The CSV contains &lt;span class=\"result\" data-language=\"sql\" data-code=\"select count(*) from orders\"&gt;6&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; orders.\n</code></pre> <p>The SQL verifier expects result queries to return a value:</p> <ul> <li>one row and one column becomes a scalar;</li> <li>multiple rows and one column become a list, useful with <code>data-compare=\"set\"</code>;</li> <li>multiple columns become tuples.</li> </ul>"},{"location":"how-to-guides/use-sql-and-csv-files/#query-a-csv-directly","title":"Query A CSV Directly","text":"<p>You can also query the CSV directly from the result span:</p> <pre><code>Paid orders total &lt;span class=\"result\" data-language=\"sql\" data-code=\"select printf('$%.2f', sum(amount)) from read_csv_auto('data/orders.csv') where status = 'paid'\"&gt;$461.00&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>For a complete example, see SQL CSV Sales Summary.</p>"},{"location":"how-to-guides/verify-a-document/","title":"Verify A Document","text":"<p>Use <code>provedown verify</code> to check one or more Provedown Markdown or HTML files.</p>"},{"location":"how-to-guides/verify-a-document/#verify-one-file","title":"Verify One File","text":"<pre><code>provedown verify report.md\n</code></pre> <p>Text output summarizes each file and prints one line per finding:</p> <pre><code>report.md: ok\n  pass=1, fail=0, skip=0, error=0\n  [pass] python-results report.md:5:16: value matches exactly expected='42' actual='42'\n</code></pre> <p>The command exits with <code>0</code> when every report is ok. A report is ok when it has no <code>fail</code> or <code>error</code> findings. <code>skip</code> findings are allowed.</p>"},{"location":"how-to-guides/verify-a-document/#verify-multiple-files","title":"Verify Multiple Files","text":"<pre><code>provedown verify report.md appendix.md\n</code></pre> <p>Each path is parsed and verified independently.</p>"},{"location":"how-to-guides/verify-a-document/#verify-html","title":"Verify HTML","text":"<p>HTML documents use the same Provedown contract:</p> <pre><code>&lt;pre&gt;&lt;code&gt;\nx = 40 + 2\n&lt;/code&gt;&lt;/pre&gt;\n\n&lt;p&gt;\n  The answer is\n  &lt;span class=\"result\" data-code=\"x\"&gt;42&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n&lt;/p&gt;\n</code></pre> <p>Verify the file the same way:</p> <pre><code>provedown verify report.html\n</code></pre>"},{"location":"how-to-guides/verify-a-document/#relative-data-files","title":"Relative Data Files","text":"<p>When verifying a file from disk, verifiers run with the document's directory as the working directory. This lets reports read nearby data files with relative paths:</p> <pre><code>&lt;pre&gt;&lt;code&gt;\nfrom pathlib import Path\n\nvalue = Path(\"data/value.txt\").read_text(encoding=\"utf-8\").strip()\n&lt;/code&gt;&lt;/pre&gt;\n\nThe value is &lt;span class=\"result\" data-code=\"value\"&gt;42&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>If the document is <code>reports/summary.md</code>, the path above resolves to <code>reports/data/value.txt</code>.</p>"},{"location":"how-to-guides/verify-a-document/#use-json-output","title":"Use JSON Output","text":"<pre><code>provedown verify --format json report.md\n</code></pre> <p>JSON output is intended for CI, editor integrations, and scripts. It includes an overall <code>ok</code> field, per-file summaries, and structured findings.</p>"},{"location":"how-to-guides/verify-a-document/#select-a-verifier","title":"Select A Verifier","text":"<p>List verifier ids:</p> <pre><code>provedown list-verifiers\n</code></pre> <p>The built-in verifiers are currently:</p> <pre><code>python-results\nsql-results\n</code></pre> <p>Run only that verifier explicitly:</p> <pre><code>provedown verify --verifier python-results report.md\n</code></pre> <p>Run only the SQL verifier explicitly:</p> <pre><code>provedown verify --verifier sql-results report.md\n</code></pre> <p>The option may be passed more than once when additional verifier plugins exist.</p>"},{"location":"how-to-guides/verify-a-document/#interpret-statuses","title":"Interpret Statuses","text":"<p><code>pass</code> means the authored claim matched the recomputed value.</p> <p><code>fail</code> means the verifier ran successfully and found a mismatch.</p> <p><code>skip</code> means the verifier deliberately did not check the claim. For example, the Python verifier skips result spans marked with <code>data-compare=\"none\"</code>.</p> <p><code>error</code> means verification could not complete for that item. Examples include a Python exception, an unknown result code reference, or an unknown comparison policy.</p>"},{"location":"how-to-guides/write-result-assertions/","title":"Write Result Assertions","text":"<p>A result assertion is the smallest Provedown contract: an authored value in prose and the code that should reproduce it.</p>"},{"location":"how-to-guides/write-result-assertions/#inline-expressions","title":"Inline Expressions","text":"<p>Use <code>data-code</code> for short Python expressions:</p> <pre><code>&lt;code&gt;\nsamples = [\"alpha\", \"beta\", \"gamma\"]\n&lt;/code&gt;\n\nThe cohort has &lt;span class=\"result\" data-code=\"len(samples)\"&gt;3&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; samples.\n</code></pre> <p>The authored value is the text inside the result span: <code>3</code>.</p> <p>The expression is <code>len(samples)</code>.</p> <p>The optional <code>&lt;span class=\"method\"&gt;&lt;/span&gt;</code> slot is ignored by the verifier. It is reserved for renderers that may later inject method markers or disclosure UI.</p>"},{"location":"how-to-guides/write-result-assertions/#exact-comparison","title":"Exact Comparison","text":"<p>Exact comparison is the default:</p> <pre><code>The answer is &lt;span class=\"result\" data-code=\"answer\"&gt;42&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>The computed value is converted to text with <code>str()</code>, then compared with the authored text.</p>"},{"location":"how-to-guides/write-result-assertions/#numeric-comparison","title":"Numeric Comparison","text":"<p>Use <code>data-compare=\"numeric\"</code> when textual representations may differ but numeric values should match:</p> <pre><code>The total is &lt;span class=\"result\" data-code=\"total\" data-compare=\"numeric\"&gt;42&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>This treats <code>42</code> and <code>42.0</code> as equal.</p>"},{"location":"how-to-guides/write-result-assertions/#tolerance-comparison","title":"Tolerance Comparison","text":"<p>Use <code>tol</code> or <code>data-tol</code> for floating-point values:</p> <pre><code>The rate is &lt;span class=\"result\" data-code=\"rate\" tol=\"1e-6\"&gt;0.333333&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>The parser assigns the <code>tol</code> comparison policy automatically when a tolerance attribute is present and <code>data-compare</code> is not set.</p>"},{"location":"how-to-guides/write-result-assertions/#set-comparison","title":"Set Comparison","text":"<p>Use <code>data-compare=\"set\"</code> for unordered collections:</p> <pre><code>The observed labels are &lt;span class=\"result\" data-code=\"labels\" data-compare=\"set\"&gt;alpha,beta,gamma&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>The Python verifier can compare authored comma-separated text, Python literal collections, and computed sets, lists, tuples, or dict keys.</p>"},{"location":"how-to-guides/write-result-assertions/#seeded-comparison","title":"Seeded Comparison","text":"<p>Use <code>seed</code> or <code>data-seed</code> when an assertion depends on Python randomness:</p> <pre><code>&lt;code&gt;\nimport random\n&lt;/code&gt;\n\nThe draw is &lt;span class=\"result\" data-code=\"random.randint(1, 10)\" seed=\"7\"&gt;6&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>The Python verifier seeds Python's <code>random</code> module. If <code>np</code> is present in the execution namespace and has <code>np.random</code>, it seeds NumPy's random generator too.</p>"},{"location":"how-to-guides/write-result-assertions/#explicitly-unverified-claims","title":"Explicitly Unverified Claims","text":"<p>Use <code>data-compare=\"none\"</code> only when a value is intentionally outside the current verification contract:</p> <pre><code>The external service status is &lt;span class=\"result\" data-code=\"status()\" data-compare=\"none\"&gt;available&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>The Python verifier does not evaluate the expression. It emits a <code>skip</code> finding.</p>"},{"location":"how-to-guides/write-result-assertions/#other-languages","title":"Other Languages","text":"<p>The markup is language-neutral. Add <code>data-language</code>, <code>language</code>, or <code>lang</code> to mark a result for another verifier:</p> <pre><code>The answer is &lt;span class=\"result\" data-code=\"select 40 + 2\" data-language=\"sql\"&gt;42&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>Built-in language names:</p> <code>python</code>, <code>py</code> Handled by <code>python-results</code>. <code>sql</code>, <code>duckdb</code>, <code>duckdb-sql</code> Handled by <code>sql-results</code>. <p>Verifiers ignore result spans for other languages. Use <code>data-compare=\"none\"</code> when a claim is intentionally unverified and should produce an explicit <code>skip</code>.</p>"},{"location":"ideas/verifiable-weave-spec/","title":"Verifiable Literate Weave \u2014 Design Spec","text":"<p>Status: draft v0.1 One line: A markdown-native document format where human-readable prose is the primary artifact, computed values are authored inline, and a cheap post-hoc pass re-executes the embedded code to verify those values \u2014 catching agent hallucination in the same session it was produced.</p>"},{"location":"ideas/verifiable-weave-spec/#1-motivation","title":"1. Motivation","text":"<p>Agentic tools that produce reports (BERIL, openscientist-style outputs) are readable but can hallucinate. Tools that produce notebooks are checkable (re-runnable) but optimize for the wrong reader: the notebook becomes the primary artifact and the prose an afterthought, so user-unfriendly Python leaks into the deliverable.</p> <p>This format inverts that. It returns to the original literate-programming claim \u2014 the document is written for humans, in the order a human wants to read it \u2014 while keeping the notebook's one genuinely valuable property: the values can be mechanically re-derived and checked.</p> <p>The target threat is specific and hot: the agent wrote this document in this session; did it fabricate a number; can I trust it right now. This is deliberately not the cold reproducibility problem (re-run a year later on another machine). Long-term reproducibility is a welcome side effect, not the goal. Scoping to hot verification removes the hardest parts of the reproducibility literature \u2014 environment reconstruction and fuzzy type-aware comparison \u2014 because the data, interpreter, and dependencies are the same ones that produced the value seconds ago.</p>"},{"location":"ideas/verifiable-weave-spec/#what-this-is-precisely","title":"What this is, precisely","text":"<p>A notebook with the cells left inline but hidden by default, the narrative promoted to primary, and the printed values pinned and asserted rather than merely displayed. Reading is the default mode; verification is a separate, optional, non-LLM pass. There is no source/derived split \u2014 the document with its inline values is the source of truth. The agent may write a value by running code, by hand, or by copying; verification re-derives it and flags any disagreement.</p>"},{"location":"ideas/verifiable-weave-spec/#2-core-model","title":"2. Core model","text":"<ol> <li>The document is the source. An inline value such as <code>412</code> is the real, primary, human-readable artifact. The attached code is annotation justifying the value, not the value's generator-of-record.</li> <li>Notebook execution semantics. Code blocks form an ordered cell sequence sharing one kernel; state accumulates linearly. A value assertion is checked against live state at its position in that sequence.</li> <li>Verification is falsification, not generation. A separate dumb (non-LLM) pass re-executes the code and asserts equality with the authored value. A mismatch is the trust signal.</li> <li>Document order = execution order, by default. A forward-reference mechanism is the only escape hatch when narrative order must diverge from execution order.</li> <li>Hot equality, not fuzzy similarity. Because verification runs in the same environment, the default comparison is exact equality. Tolerance/normalization policies exist only for genuinely non-deterministic values, and declaring one is a visible, auditable, lint-able choice.</li> </ol>"},{"location":"ideas/verifiable-weave-spec/#two-failure-modes-one-signal","title":"Two failure modes, one signal","text":"<p>A mismatch means either (a) the agent hallucinated/miscopied the value, or (b) the document is stale. Both mean \"the document no longer tells the truth,\" which is exactly the signal wanted. They need not be distinguished for the trust use case.</p>"},{"location":"ideas/verifiable-weave-spec/#3-markup","title":"3. Markup","text":""},{"location":"ideas/verifiable-weave-spec/#31-the-result-assertion-the-unit-that-matters","title":"3.1 The result assertion (the unit that matters)","text":"<p>The agent's entire contract is: a value, the code that derives it, and (optionally) a comparison policy. Nothing else.</p> <pre><code>The cohort has &lt;span class=\"result\" data-code=\"len(df)\"&gt;412&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; samples.\n</code></pre> <ul> <li><code>class=\"result\"</code> carries the contract. The verifier keys off this.</li> <li><code>data-code</code> \u2014 the expression whose result must equal the span's authored text. Short code inline; long code by reference (\u00a73.3).</li> <li><code>data-compare</code> (optional, default <code>exact</code>) \u2014 comparison policy (\u00a74).</li> <li><code>412</code> \u2014 the authored value. Primary, human-readable, asserted.</li> <li><code>&lt;span class=\"method\"&gt;&lt;/span&gt;</code> \u2014 an empty presentation slot. The agent never fills it. A preprocessor injects the citation mark / disclosure affordance (\u00a75). Keeping it empty means the agent maintains no numbering and no footnote table \u2014 the hallucination-resistant contract stays as small as possible.</li> </ul> <p><code>&lt;span&gt;</code> (not a custom <code>&lt;method&gt;</code> element) is chosen so every markdown renderer and HTML sanitizer passes it through unchanged.</p>"},{"location":"ideas/verifiable-weave-spec/#32-code-cells","title":"3.2 Code cells","text":"<p>By default, code lives in <code>&lt;code&gt;</code> blocks woven through the document. Cell order is document order; the document is the notebook, linearized.</p> <pre><code>&lt;code name=\"load\"&gt;\ndf = pd.read_parquet(\"cohort.parquet\")\n&lt;/code&gt;\n</code></pre> <ul> <li>A <code>&lt;code&gt;</code> block executes in document order against the shared kernel.</li> <li>An optional <code>name</code> makes the block referenceable (needed only for forward references).</li> <li>Whether a <code>&lt;code&gt;</code> block is visible by default is a presentation choice (\u00a75) \u2014 the verifier runs it regardless.</li> </ul>"},{"location":"ideas/verifiable-weave-spec/#33-inline-vs-referenced-code","title":"3.3 Inline vs. referenced code","text":"<ul> <li>Inline (<code>data-code=\"len(df)\"</code>) \u2014 for short single expressions that fit readably in an attribute.</li> <li>Referenced (<code>data-code=\"#cohort-size\"</code>) \u2014 for anything longer. The expression lives in a named <code>&lt;code&gt;</code> block; the result span points at it. This is the original <code>&lt;exec code=\"#script123\"&gt;</code> idea as the overflow path.</li> </ul> <pre><code>The cohort has &lt;span class=\"result\" data-code=\"#cohort-size\"&gt;412&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; samples.\n\n&lt;!-- in methods, or woven nearby --&gt;\n&lt;code name=\"cohort-size\"&gt;len(df)&lt;/code&gt;\n</code></pre> <p>Threshold rule of thumb: if the expression is a single side-effect-free expression under ~40 chars, inline it; otherwise name it and reference it.</p>"},{"location":"ideas/verifiable-weave-spec/#34-forward-references-the-only-ordering-escape-hatch","title":"3.4 Forward references (the only ordering escape hatch)","text":"<p>Default: document order = execution order, zero ceremony. When prose order must diverge from execution order, name a block and use it elsewhere \u2014 the literate-programming <code>@&lt;...@&gt;</code> move.</p> <pre><code>&lt;code name=\"load\"&gt;df = pd.read_parquet(\"cohort.parquet\")&lt;/code&gt;\n...prose that needs df to already exist...\n&lt;code use=\"load\"/&gt;\n</code></pre> <p>Ordering contract: the use site is the execution site. The definition is parked text; execution order is the order of use sites (and of inline <code>&lt;code&gt;</code> blocks, which are their own use site). This preserves a single linear execution sequence, so a <code>result</code> span always asserts against well-defined state.</p>"},{"location":"ideas/verifiable-weave-spec/#4-comparison-policies","title":"4. Comparison policies","text":"<p>In hot verification the default is exact string/value equality, because re-running against identical state is deterministic. Policies exist only for the cases where exact equality is legitimately wrong, and declaring one is auditable \u2014 an agent marking something <code>none</code> that is actually deterministic is making a visible choice you can lint for (the way you lint for swallowed exceptions).</p> <code>data-compare</code> Meaning When <code>exact</code> (default) byte/value equality almost everything in-session <code>numeric</code> parse as number, compare equal <code>412</code> vs <code>412.0</code> <code>tol=\"1e-6\"</code> numeric within tolerance floats <code>set</code> order-insensitive collection equality sets, dict keys, unordered rows <code>seed=\"42\"</code> RNG pinned before exec, then exact stochastic-but-pinnable <code>none</code> not verified; explicitly out of scope wall-clock, external API, true nondeterminism <p><code>none</code> does not undermine the checksum as long as it is declared and countable. The escape hatches are visible in the document; a lint can flag \"too many <code>none</code>.\"</p>"},{"location":"ideas/verifiable-weave-spec/#5-rendering-progressive-disclosure","title":"5. Rendering &amp; progressive disclosure","text":"<p>The design target is that default rendering in standard markdown tools is already friendly, with richer disclosure available via light CSS, and full interactivity in a bespoke renderer. Superlight preprocessing produces whichever view is wanted; all views are a pure function of the authored source.</p> <p>Because the format already assumes a preprocessing step, it does not rely on markdown footnote (<code>[^1]</code>) syntax \u2014 it weaves freely in HTML. The empty <code>method</code> slot is filled mechanically:</p> <ul> <li>Citation mark. Injected as a small superscript marker. Marks attach to the claim (after the noun), not jammed against the digits \u2014 <code>412 samples\u00b9</code>, never <code>412\u00b9</code>, which reads as an exponent. A bracketed/spaced mark (<code>[1]</code>) is unambiguous against numeric values.</li> <li>Disclosure. Code is hidden by default and revealed on demand. With light CSS this is a click-to-expand (<code>&lt;label&gt;</code>/checkbox, no JS); the degraded no-CSS case still shows the value plus a small mark.</li> <li>Methods placement. Code may be woven inline (for code-literate readers) or pooled in a methods section at the end (clean weave). Both are generated from the same <code>data-code</code> references; this is purely a layout choice and does not affect execution order except via forward references.</li> </ul> <p>Numbering, mark placement, footnote/methods pooling, and CSS injection are all mechanical \u2014 owned by the preprocessor, never authored. The agent emits only value + code + (optional) policy.</p>"},{"location":"ideas/verifiable-weave-spec/#6-the-verifier","title":"6. The verifier","text":"<p>A small, non-LLM, post-hoc consumer of the document. Where it runs is incidental \u2014 a Claude Code stop-hook, a GitHub Action, or a manual CLI invocation \u2014 because the document is the contract and the trigger is just deployment. It is cheap and easy by design.</p> <p>Algorithm:</p> <ol> <li>Parse the document; collect <code>&lt;code&gt;</code> blocks and <code>result</code> spans in document order.</li> <li>Resolve forward references into a single linear cell sequence (use sites are execution sites).</li> <li>Start one fresh kernel. Execute cells in sequence.</li> <li>At each <code>result</code> span's execution point, evaluate its <code>data-code</code> against current live state and compare to the authored value under the span's <code>data-compare</code> policy.</li> <li>Emit a per-span pass/fail report. Optionally annotate the document in place.</li> </ol> <p>Note on \"hot\" vs the kernel: a stop-hook or CI job runs in a fresh process \u2014 the agent's live kernel is gone. So even hot verification re-establishes state by running the cells from the top. The difference from cold reproducibility is that this happens on the same machine, same files, same dependencies, seconds later \u2014 so it is cheap, but it is not stateless: cells must run in order so that a <code>result</code> span sees the state it was authored against. This is the one piece of \"setup\" that survives, and it is exactly the notebook execution model, nothing more.</p> <p>Failure surface (deployment choice):</p> <ul> <li>Fatal \u2014 hook blocks the turn / CI fails the build. Use when no unverified values may ship.</li> <li>Advisory \u2014 annotate the document with pass/fail marks and let a human judge. Use when <code>none</code> escape hatches are legitimate and human review is expected.</li> </ul>"},{"location":"ideas/verifiable-weave-spec/#7-relationship-to-prior-work","title":"7. Relationship to prior work","text":"<p>The substrate and the pieces are old; the synthesis is not.</p> <ul> <li>Literate programming (Knuth, 1984) \u2014 document written for humans, machine-extraction secondary. This format restores that priority; BERIL-style notebook-first output had inverted it.</li> <li>Live values in prose (knitr / Quarto inline code, ~2012) \u2014 narrative pulls computed values from execution. But these generate the value fresh at render time; there is no authored value for a pass to disagree with, so nothing catches a fabricated number. This format makes the value primary and authored, and verification a falsification pass.</li> <li>Reproducibility checking (Jupyter-from-PMC studies; SRI similarity index) \u2014 \"recompute and diff against the printed value\" at scale, plus type-aware comparison tolerances. Same mechanism, but framed as cold reproducibility (rerun later, environment drift is the core problem) and as a continuous similarity score. This format borrows the comparison taxonomy but scopes to hot, in-session, exact-equality verification, where their hardest problem \u2014 environment reconstruction, which their data shows is what actually kills automated reruns \u2014 largely vanishes.</li> <li>Agent tool-receipt verification (2025\u201326) \u2014 cross-check LLM claims against signed records of what tools actually returned; re-fetch to catch fabricated URLs. Structurally the same anti-hallucination move (verify-by-recompute, give actionable trust signals rather than binary verified/unverified) applied to tool calls rather than to a readable document.</li> <li>Prior art to clear: a USPTO patent on \"determining validity of multipart branching literate programs\" (per-module output validation with declared inter-module dependencies). Coming from a formal-validation angle, not a readable-document one \u2014 worth reading for scope before publishing.</li> </ul> <p>One-line positioning: prior work verifies notebooks after publication; this makes the publishable artifact self-verifying by construction \u2014 the weave is born verifiable rather than checked after the fact.</p>"},{"location":"ideas/verifiable-weave-spec/#8-open-questions-decisions-deferred","title":"8. Open questions / decisions deferred","text":"<ul> <li>LinkML schema for the contract. The <code>result</code> / <code>code</code> units (value, code-or-ref, compare policy, name/use) are the shared contract among agent, renderer, and verifier. A schema is what makes \"stuff leaking through\" enforceable by a build step rather than by prompting. Deferred until the markup stabilizes.</li> <li>Result types beyond scalars. Scalars round-trip trivially inline. Tables, dataframes, and figures need a discipline (referenced artifact + hash) if they are ever to be asserted. v1 scope is scalar-in-prose; richer result types are future work.</li> <li>Verification status in the document. Whether the citation mark reflects verified state (green check / red x) or the document stays verification-state-free and status lives only in the verifier's output. Folding status into the mark bakes a \"verified-at\" notion into the doc; keeping it out keeps the doc a pure source. Leaning state-free for v1.</li> <li><code>none</code> budget / linting. Define a lint that flags an excess of <code>data-compare=\"none\"</code> so escape hatches stay countable.</li> </ul>"},{"location":"reference/cli/","title":"CLI Reference","text":"<p>The command-line entry point is <code>provedown</code>.</p>"},{"location":"reference/cli/#provedown-verify","title":"<code>provedown verify</code>","text":"<p>Verify one or more documents.</p> <pre><code>provedown verify [--verifier VERIFIER_ID] [--format text|json] PATH [PATH ...]\n</code></pre> <p>Arguments:</p> <code>PATH</code> Markdown or HTML document to parse and verify. Multiple paths are allowed. <p>Options:</p> <code>--verifier VERIFIER_ID</code> Run only the named verifier. May be passed more than once. <code>--format text</code> Print human-readable output. This is the default. <code>--format json</code> Print structured JSON output. <p>Exit codes:</p> <code>0</code> Every report is ok. Reports with only <code>pass</code> and <code>skip</code> findings are ok. <code>1</code> At least one report has a <code>fail</code> or <code>error</code> finding. <code>2</code> Argument parsing failed."},{"location":"reference/cli/#provedown-list-verifiers","title":"<code>provedown list-verifiers</code>","text":"<p>List verifier ids registered in the default registry.</p> <pre><code>provedown list-verifiers\n</code></pre> <p>Current output:</p> <pre><code>python-results\nsql-results\n</code></pre>"},{"location":"reference/cli/#provedown-inspect","title":"<code>provedown inspect</code>","text":"<p>Inspect code and result relationships without executing document code.</p> <pre><code>provedown inspect [--format text|json] PATH [PATH ...]\n</code></pre> <p>Arguments:</p> <code>PATH</code> Markdown or HTML document to parse and inspect. Multiple paths are allowed. <p>Options:</p> <code>--format text</code> Print human-readable inspection output. This is the default. <code>--format json</code> Print structured JSON output. <p>Exit codes:</p> <code>0</code> No inspection errors were found. Warnings are allowed. <code>1</code> At least one inspection error was found, such as an unresolved code use or   result reference. <code>2</code> Argument parsing failed."},{"location":"reference/cli/#provedown-lint","title":"<code>provedown lint</code>","text":"<p>Lint Provedown documents without executing document code.</p> <pre><code>provedown lint [--format text|json] PATH [PATH ...]\n</code></pre> <p>Arguments:</p> <code>PATH</code> Markdown or HTML document to parse and lint. Multiple paths are allowed. <p>Options:</p> <code>--format text</code> Print human-readable lint output. This is the default. <code>--format json</code> Print structured JSON output. <p>Exit codes:</p> <code>0</code> No lint errors were found. Warnings are allowed. <code>1</code> At least one lint error was found. <code>2</code> Argument parsing failed."},{"location":"reference/cli/#verify-text-output","title":"Verify Text Output","text":"<p>Text output has one file summary followed by finding lines:</p> <pre><code>report.md: ok\n  pass=1, fail=0, skip=0, error=0\n  [pass] python-results report.md:5:16: value matches exactly expected='42' actual='42'\n</code></pre>"},{"location":"reference/cli/#verify-json-output-shape","title":"Verify JSON Output Shape","text":"<p>JSON output has an overall <code>ok</code> field and one report object per path:</p> <pre><code>{\n  \"ok\": true,\n  \"reports\": [\n    {\n      \"path\": \"report.md\",\n      \"ok\": true,\n      \"summary\": {\n        \"pass\": 1,\n        \"fail\": 0,\n        \"skip\": 0,\n        \"error\": 0\n      },\n      \"findings\": []\n    }\n  ]\n}\n</code></pre> <p>Findings include <code>verifier_id</code>, <code>status</code>, <code>location</code>, <code>message</code>, <code>expected</code>, <code>actual</code>, and <code>evidence</code>.</p>"},{"location":"reference/cli/#python-execution-directory","title":"Python Execution Directory","text":"<p>When <code>provedown verify</code> reads a document from disk, the built-in Python verifier executes code with that document's parent directory as the working directory. Relative paths therefore resolve next to the document being verified.</p> <p>For example, code in <code>reports/summary.md</code> can read <code>reports/data/input.csv</code> as <code>Path(\"data/input.csv\")</code>.</p>"},{"location":"reference/frontmatter/","title":"Frontmatter","text":"<p>Markdown documents may start with YAML frontmatter. Provedown parses it, exposes it on the Python API, and ignores it when looking for executable code and result assertions.</p> <pre><code>---\ntitle: Sales summary\nowner: analytics\ntags:\n  - weekly\n  - revenue\nprovedown:\n  aliases:\n    data: data\n    cache: ../cache\n  last_validated: \"2026-06-15\"\n  default_language: python\n  pyproject: pyproject.toml\n---\n</code></pre>"},{"location":"reference/frontmatter/#user-metadata","title":"User Metadata","text":"<p>The top-level YAML schema belongs to the project that owns the document. Provedown does not validate <code>title</code>, <code>owner</code>, <code>tags</code>, or other user-defined fields. Unknown fields are preserved in <code>Document.frontmatter</code>.</p> <p>If the YAML cannot be parsed, Provedown emits a parser diagnostic. That diagnostic becomes an <code>error</code> finding in <code>provedown verify</code>.</p>"},{"location":"reference/frontmatter/#provedown-block","title":"<code>provedown</code> Block","text":"<p>The optional <code>provedown</code> mapping is reserved for Provedown-aware tooling.</p> <code>aliases</code> Optional mapping of short names to data folders or other local paths. The   parser stores these aliases for verifiers and integrations. <code>last_validated</code> Optional date or string recording when the document was last validated. YAML   dates are normalized to strings in <code>Document.provedown.last_validated</code>. <code>default_language</code> Optional default language for <code>&lt;code&gt;</code>, <code>&lt;code use=\"...\"/&gt;</code>, and result   spans that do not set <code>data-language</code>, <code>language</code>, or <code>lang</code>. Defaults to   <code>python</code>. <code>pyproject</code> Optional path to the Python project's <code>pyproject.toml</code>, relative to the   document or repository convention used by the verifier. <code>pyproject_toml</code> Accepted as a synonym for <code>pyproject</code>. <p>The built-in parser also accepts <code>data_aliases</code> as a synonym for <code>aliases</code>. Element-level language attributes still override <code>default_language</code>.</p>"},{"location":"reference/markup/","title":"Markup Contract","text":"<p>Provedown looks for a small HTML contract in Markdown or HTML documents.</p> <p>The parser currently recognizes <code>&lt;code&gt;</code> elements, <code>&lt;code use=\"...\"/&gt;</code> elements, and <code>&lt;span class=\"result\"&gt;</code> elements.</p>"},{"location":"reference/markup/#source-formats","title":"Source Formats","text":"<p>Markdown documents can mix prose with raw Provedown HTML:</p> <pre><code>The answer is &lt;span class=\"result\" data-code=\"x\"&gt;42&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>HTML documents use the same contract directly:</p> <pre><code>&lt;pre&gt;&lt;code name=\"calc\"&gt;\nx = 40 + 2\n&lt;/code&gt;&lt;/pre&gt;\n&lt;p&gt;\n  The answer is\n  &lt;span class=\"result\" data-code=\"x\"&gt;42&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n&lt;/p&gt;\n</code></pre> <p>Markdown fenced code blocks and YAML frontmatter are ignored when scanning for Provedown HTML. HTML regions with <code>data-provedown-ignore=\"true\"</code> or the <code>provedown-ignore</code> class are also ignored.</p> <p>In source HTML, every <code>&lt;code&gt;</code> element is treated as Provedown code. Put non-executable code samples inside an ignored region when they should only be shown to readers.</p>"},{"location":"reference/markup/#code-blocks","title":"Code Blocks","text":"<p>Use <code>&lt;code&gt;</code> for executable cells:</p> <pre><code>&lt;code&gt;\nx = 40 + 2\n&lt;/code&gt;\n</code></pre> <p>Attributes:</p> <code>name</code> Optional block name. <code>data-language</code>, <code>language</code>, <code>lang</code> Optional language marker. Defaults to the document's <code>provedown.default_language</code>   frontmatter setting, or <code>python</code> when unset. <p>Built-in verifier language names are <code>python</code>/<code>py</code> and <code>sql</code>/<code>duckdb</code>/<code>duckdb-sql</code>.</p> <p>Unknown attributes are preserved in the parsed IR.</p> <p>The parser strips leading and trailing newlines from the code text.</p>"},{"location":"reference/markup/#named-code","title":"Named Code","text":"<p>Name a block with <code>name</code>:</p> <pre><code>&lt;code name=\"load\"&gt;\nx = 40 + 2\n&lt;/code&gt;\n</code></pre> <p>Named blocks can be referenced by result assertions or executed at use sites.</p> <p>Duplicate names are accepted, but the later definition wins and the parser emits a diagnostic.</p>"},{"location":"reference/markup/#code-uses","title":"Code Uses","text":"<p>Use a named block at a specific execution point:</p> <pre><code>&lt;code use=\"load\"/&gt;\n</code></pre> <p>The <code>use</code> attribute names the block to execute. Empty uses emit diagnostics.</p>"},{"location":"reference/markup/#result-assertions","title":"Result Assertions","text":"<p>Use <code>&lt;span class=\"result\"&gt;</code> for authored scalar claims:</p> <pre><code>The answer is &lt;span class=\"result\" data-code=\"x\"&gt;42&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt;.\n</code></pre> <p>Attributes:</p> <code>class=\"result\"</code> Marks the span as a Provedown assertion. <code>data-code</code> Python expression to evaluate, or <code>#name</code> to reference a named code block. <code>data-compare</code> Optional comparison policy. Defaults to <code>exact</code>. <code>data-language</code>, <code>language</code>, <code>lang</code> Optional language marker. Defaults to the document's <code>provedown.default_language</code>   frontmatter setting, or <code>python</code> when unset. <p>Built-in verifier language names are <code>python</code>/<code>py</code> and <code>sql</code>/<code>duckdb</code>/<code>duckdb-sql</code>.</p> <code>tol</code>, <code>data-tol</code> Numeric tolerance. If present without <code>data-compare</code>, the comparison policy is   <code>tol</code>. <code>seed</code>, <code>data-seed</code> Random seed. If present without <code>data-compare</code>, the comparison policy is   <code>seed</code>. <p>Unknown attributes are preserved in the parsed IR.</p> <p>The parser strips surrounding whitespace from the authored value.</p>"},{"location":"reference/markup/#method-slot","title":"Method Slot","text":"<p>Result spans may contain an empty method slot:</p> <pre><code>&lt;span class=\"method\"&gt;&lt;/span&gt;\n</code></pre> <p>The parser ignores text inside the method slot when collecting the authored value. Future renderers can use this slot for method markers or disclosure UI.</p>"},{"location":"reference/markup/#diagnostics","title":"Diagnostics","text":"<p>Parser diagnostics are converted into <code>error</code> findings by <code>provedown verify</code>.</p> <p>Current diagnostics include:</p> <ul> <li>nested HTML tags inside <code>&lt;code&gt;</code>;</li> <li>duplicate code block names;</li> <li>empty code uses;</li> <li>unclosed <code>&lt;code&gt;</code> blocks;</li> <li>unclosed result spans;</li> <li>result spans missing <code>data-code</code>.</li> </ul> <p>See Frontmatter for document-level metadata and defaults.</p>"},{"location":"reference/python-api/","title":"Python API Reference","text":"<p>The public API is exported from <code>provedown</code>.</p>"},{"location":"reference/python-api/#parse-documents","title":"Parse Documents","text":"<p>Parse Markdown or HTML text:</p> <pre><code>from provedown import parse_document\n\ndocument = parse_document(\"\"\"\n&lt;code&gt;x = 40 + 2&lt;/code&gt;\nThe answer is &lt;span class=\"result\" data-code=\"x\"&gt;42&lt;/span&gt;.\n\"\"\")\n</code></pre> <p>Parse a file:</p> <pre><code>from pathlib import Path\nfrom provedown import parse_file\n\ndocument = parse_file(Path(\"report.md\"))\n</code></pre> <p>The parsed <code>Document</code> contains:</p> <code>source</code> Original source text. <code>path</code> Optional source path. <code>events</code> Ordered <code>CodeBlock</code>, <code>CodeUse</code>, and <code>ResultAssertion</code> events. <code>named_code</code> Mapping of code block names to <code>CodeBlock</code> objects. <code>frontmatter</code> Parsed YAML frontmatter from Markdown documents. User-defined fields are   preserved but not validated by Provedown. <code>provedown</code> Normalized Provedown-specific frontmatter settings, including <code>aliases</code>,   <code>last_validated</code>, <code>default_language</code>, and <code>pyproject</code>. <code>diagnostics</code> Parser diagnostics."},{"location":"reference/python-api/#verify-documents","title":"Verify Documents","text":"<p>Verify parsed text:</p> <pre><code>from provedown import parse_document, verify_document\n\ndocument = parse_document(\"\"\"\n&lt;code&gt;x = 40 + 2&lt;/code&gt;\nThe answer is &lt;span class=\"result\" data-code=\"x\"&gt;42&lt;/span&gt;.\n\"\"\")\n\nreport = verify_document(document)\nassert report.ok\n</code></pre> <p>Verify a file:</p> <pre><code>from pathlib import Path\nfrom provedown import verify_file\n\nreport = verify_file(Path(\"report.md\"))\n</code></pre> <p><code>verify_file()</code> runs with the document directory as the working directory.</p>"},{"location":"reference/python-api/#inspect-reports","title":"Inspect Reports","text":"<pre><code>from provedown import Status\n\nsummary = report.summary()\nfailures = report.count(Status.FAIL)\npayload = report.to_dict()\n</code></pre> <p>A report is ok when it contains no <code>fail</code> or <code>error</code> findings.</p>"},{"location":"reference/python-api/#register-a-custom-verifier","title":"Register A Custom Verifier","text":"<pre><code>from collections.abc import Iterable\n\nfrom provedown import (\n    Document,\n    Finding,\n    SourceLocation,\n    Status,\n    VerificationContext,\n    VerifierRegistry,\n    verify_document,\n)\n\n\nclass AlwaysPassVerifier:\n    verifier_id = \"always-pass\"\n\n    def verify(\n        self,\n        document: Document,\n        context: VerificationContext,\n    ) -&gt; Iterable[Finding]:\n        del context\n        yield Finding(\n            verifier_id=self.verifier_id,\n            status=Status.PASS,\n            location=SourceLocation(path=document.path, line=1, column=1),\n            message=\"custom verifier ran\",\n        )\n\n\nregistry = VerifierRegistry()\nregistry.register(AlwaysPassVerifier())\n\nreport = verify_document(\n    Document(source=\"\", path=None, events=[], named_code={}),\n    registry=registry,\n)\n</code></pre> <p>Verifier plugins receive the parsed document IR and return portable findings.</p>"},{"location":"reference/verifier-plugins/","title":"Verifier Plugins","text":"<p>Verifier plugins let Provedown check more than Python scalar results. A verifier receives a parsed <code>Document</code> and returns <code>Finding</code> objects.</p>"},{"location":"reference/verifier-plugins/#protocol","title":"Protocol","text":"<p>A verifier has a stable id and a <code>verify()</code> method:</p> <pre><code>from collections.abc import Iterable\n\nfrom provedown import Document, Finding, VerificationContext\n\n\nclass MyVerifier:\n    verifier_id = \"my-verifier\"\n\n    def verify(\n        self,\n        document: Document,\n        context: VerificationContext,\n    ) -&gt; Iterable[Finding]:\n        ...\n</code></pre>"},{"location":"reference/verifier-plugins/#findings","title":"Findings","text":"<p>Each finding has:</p> <code>verifier_id</code> The verifier that produced the finding. <code>status</code> One of <code>pass</code>, <code>fail</code>, <code>skip</code>, or <code>error</code>. <code>location</code> Source location for the relevant document item. <code>message</code> Human-readable explanation. <code>expected</code> Optional authored value or expectation. <code>actual</code> Optional computed value. <code>evidence</code> Optional structured metadata."},{"location":"reference/verifier-plugins/#status-semantics","title":"Status Semantics","text":"<p>Use <code>pass</code> when the verifier checked the item and it matched.</p> <p>Use <code>fail</code> when the verifier checked the item and found a mismatch.</p> <p>Use <code>skip</code> when the verifier intentionally did not check the item.</p> <p>Use <code>error</code> when the verifier could not complete the check.</p> <p>Only <code>fail</code> and <code>error</code> make a report not ok.</p>"},{"location":"reference/verifier-plugins/#context","title":"Context","text":"<p><code>VerificationContext</code> currently provides:</p> <code>cwd</code> Optional working directory for verifier execution. <code>comparators</code> Comparator registry used by scalar result verifiers. <p>Verifiers may ignore context fields that do not apply.</p>"},{"location":"reference/verifier-plugins/#registration","title":"Registration","text":"<p>Create a registry and register verifier instances:</p> <pre><code>from provedown import VerifierRegistry\n\nregistry = VerifierRegistry()\nregistry.register(MyVerifier())\n</code></pre> <p>Then pass the registry to <code>verify_document()</code>.</p> <p>The default registry currently includes:</p> <ul> <li><code>python-results</code> for <code>python</code> and <code>py</code> code;</li> <li><code>sql-results</code> for <code>sql</code>, <code>duckdb</code>, and <code>duckdb-sql</code> code.</li> </ul>"},{"location":"tutorials/first-verified-report/","title":"First Verified Report","text":"<p>This tutorial creates a tiny Provedown document and verifies that a prose claim matches the code attached to it.</p>"},{"location":"tutorials/first-verified-report/#prerequisites","title":"Prerequisites","text":"<p>Install Provedown, then use the <code>provedown</code> command in your shell.</p>"},{"location":"tutorials/first-verified-report/#create-a-report","title":"Create A Report","text":"<p>Create a file named <code>report.md</code>:</p> <pre><code># Example Report\n\n&lt;code&gt;\nsamples = [\"alpha\", \"beta\", \"gamma\"]\nsample_count = len(samples)\n&lt;/code&gt;\n\nThe cohort has &lt;span class=\"result\" data-code=\"sample_count\"&gt;3&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; samples.\n</code></pre> <p>The document is ordinary Markdown plus two Provedown contracts:</p> <ul> <li><code>&lt;code&gt;</code> contains Python that will run during verification.</li> <li><code>&lt;span class=\"result\"&gt;</code> contains an authored prose value and the expression   that should reproduce it.</li> </ul>"},{"location":"tutorials/first-verified-report/#verify-it","title":"Verify It","text":"<p>Run:</p> <pre><code>provedown verify report.md\n</code></pre> <p>Expected text output:</p> <pre><code>report.md: ok\n  pass=1, fail=0, skip=0, error=0\n  [pass] python-results report.md:7:16: value matches exactly expected='3' actual='3'\n</code></pre> <p>The exact line and column can differ if your file has different spacing.</p>"},{"location":"tutorials/first-verified-report/#see-a-failure","title":"See A Failure","text":"<p>Change the authored value from <code>3</code> to <code>4</code>:</p> <pre><code>The cohort has &lt;span class=\"result\" data-code=\"sample_count\"&gt;4&lt;span class=\"method\"&gt;&lt;/span&gt;&lt;/span&gt; samples.\n</code></pre> <p>Run verification again:</p> <pre><code>provedown verify report.md\n</code></pre> <p>The command exits with status <code>1</code> and reports a failed assertion:</p> <pre><code>report.md: failed\n  pass=0, fail=1, skip=0, error=0\n  [fail] python-results report.md:7:16: value differs expected='4' actual='3'\n</code></pre> <p>The verifier does not rewrite the document. It tells you that the document's claim is false under the current code and environment.</p>"},{"location":"tutorials/first-verified-report/#what-you-have","title":"What You Have","text":"<p>You now have a readable Markdown report whose scalar claim can be checked by a non-LLM verifier. That is the core Provedown loop:</p> <ol> <li>Write prose for people.</li> <li>Attach minimal executable evidence.</li> <li>Recompute and compare the authored claim.</li> </ol>"}]}